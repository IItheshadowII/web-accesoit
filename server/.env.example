# Copy to .env and fill values
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=gpt-4o

# Optional: SMTP for contact emails
SMTP_SERVICE=gmail
SMTP_USER=
SMTP_PASS=

# Database: SQLite default path is configured in prisma schema
DATABASE_URL="file:./dev.db"